# 開発エージェント向けガイド

このファイルはリポジトリ全体に適用します。ユーザーからの明示的な指示を優先してください。

## プロジェクト概要

日本加速器学会の会長選挙・代議員選挙を運営するDjangoアプリケーションです。
管理委員用の管理画面、Django管理サイト、メールの専用URLを使った電子投票、書面票入力、
開票・抽選・結果CSV出力を提供します。画面・エラーメッセージ・ドキュメントは原則日本語です。

最初に関連箇所のコードと以下の資料を確認してください。

- `README.md`: 業務の流れと操作方法
- `docs/architecture.md`: コードの責務と検証方法
- `deploy/README.md`: 本番構成とデプロイ手順
- `requirements.txt`: 固定されたPython依存関係

## 作業の進め方

- 着手時に `git status --short` と関連する差分を確認し、既存の変更を保ってください。
- 依頼に必要な範囲で変更し、無関係な整形や依存ライブラリの更新を混ぜないでください。
- リファクタリングでは既存URL、URL名、権限制御、画面表示、CSV仕様を維持してください。
- 新しい業務ルールや操作方法を追加した場合は、対応するテストとドキュメントも更新してください。
- 完了時には変更内容、実行した検証、未確認の点を簡潔に報告してください。

## コードの責務

- `election/views.py`: 投票者向け画面と投票セッション・確定処理
- `election/management_views.py`: 管理委員向け画面のHTTP入力・権限確認
- `election/auth_views.py`: 共通ログインとパスワード管理
- `election/urls.py`: 公開URLの入り口。認証・管理用の定義は `auth_urls.py` と `management_urls.py`
- `election/admin.py`: Django管理サイト
- `election/forms.py`: フォームと入力検証
- `election/permissions.py`: 担当年度へのアクセス制御
- `election/services/`: 画面と管理コマンドで共有する業務処理
- `election/responses.py`: 結果CSVのHTTPレスポンス生成
- `election/management/commands/`: 運用向けコマンド

フォームやサービスからビューをインポートしないでください。共有する投票ルールは
`services/voting.py`、投票トークンのハッシュ処理は `services/tokens.py` に置きます。
管理画面とDjango管理サイトの結果CSVは共通の生成処理を使ってください。

## 維持すべき仕様

- 管理画面は有効なスタッフアカウント向けです。担当年度とスーパーユーザーの権限を区別し、
  リンクの非表示だけでなくサーバー側でも権限を検証してください。
- ログイン画面は `/accounts/login/` に集約します。`/admin/login/` はそこへ転送し、
  戻り先URLの安全性はDjangoのログイン処理で検証します。
- ヘッダーは「姓 名」を優先し、両方未設定の場合にユーザー名を表示します。
- パスワード再設定はDjango標準のトークン・検証を利用します。メールアドレスの登録有無を
  画面の応答で知らせず、既存のパスワード検証とCSRF保護を維持してください。
- 有権者の投票済み情報と匿名の投票内容を分離し、誰が誰に投票したかを紐づけないでください。
- 電子投票の最終確定では、トランザクション内の有権者ロックと、投票済み・受付期間・
  候補者の有効性の再検証を維持してください。
- 書面票の登録は選挙のロック内で受付済み人数を確認し、受付人数を超える登録を防いでください。
- 選挙種別・予備選挙／本選挙・一般枠／企業枠ごとの候補者資格と人数制限を維持してください。
- 候補者の表示順は同じ有権者の再読み込みで変わらないようにしてください。
- 結果CSVは開票済みかつ未実行の抽選がない場合に提供します。BOM、列順、ファイル名、
  予備選挙と本選挙の判定の違いを保ってください。

## 画面とスタイル

既存のテンプレート継承と共通部品を利用してください。管理画面のスタイルは
`election/static/election/management.css`、投票画面は `voting.css` にあります。
ヘッダーのユーザーメニューは `templates/election/includes/account_nav.html` を使います。
日本語表示、モバイルでの操作、フォームのラベル、キーボード操作を確認してください。

## 開発環境と検証

リポジトリのルートから、既存の仮想環境 `.venv` を使います。
`manage.py` の標準設定は `config.settings.development` です。開発DBはSQLite、
メールはコンソール出力です。環境変数で本番設定が選択されていないか確認してください。

```bash
.venv/bin/python manage.py check
.venv/bin/python manage.py test --noinput
.venv/bin/python manage.py makemigrations --check --dry-run
git diff --check
```

変更に応じた検証を行い、文書だけの変更ではアプリケーションのテスト実行は不要です。
処理の変更ではまず該当テスト、複数機能にまたがる変更では全テストを実行してください。

- `election/tests.py`: 管理画面・権限・書面票・開票・結果出力など
- `election/test_auth.py`: ログイン・パスワード管理・ユーザー表示
- `election/test_voting.py`: 電子投票の受付・確定・再送信拒否

モデル変更が必要な場合はマイグレーションを作成し、既存データとの互換性も確認します。
本番はPostgreSQLです。SQLiteでのテスト成功を、同時実行時のDBロック検証の代わりにしないでください。

## 運用データと外部への作用

開発・テストには合成データを使ってください。会員名簿、実メールアドレス、秘密鍵、SMTP認証情報、
生の投票トークンをコード・テスト・ログ・コミットに含めないでください。
投票URL `/v/<token>/` を記録するログを追加する場合はトークンをマスクしてください。

実メール送信、開票確定、抽選実行、データ削除、デプロイは単なる動作確認として実行せず、
依頼で認められた対象・環境・範囲で行ってください。通常のコード変更やテスト実行のために、
本番DBやリポジトリ内の既存 `db.sqlite3` を初期化しないでください。
